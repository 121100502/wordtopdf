# Word批量转PDF工具｜Figma风UI界面设计规范文档

## 一、文档概述

本文档为【轻文 · Word批量转PDF离线工具】专属UI设计落地规范，基于WPF\+HandyControl技术栈打造，严格遵循Figma极简商务设计风格。统一界面视觉、布局、控件样式、交互逻辑，完全适配办公类工具属性，区别于市面粗糙免费工具，主打**干净、精致、轻量化、高质感**，为产品差异化溢价提供视觉支撑。

本规范可直接用于XAML样式编写、页面开发、视觉验收，全程可落地、无虚设设计。

## 二、整体设计理念

- **风格定位**：现代极简Figma商务风、低饱和、高留白、轻层级、无冗余装饰

- **设计核心**：功能优先、视觉轻量化、交互直观，兼顾颜值与办公实用性

- **差异化优势**：摒弃传统Windows老旧控件样式，统一圆角、阴影、配色、间距，媲美付费桌面端软件质感

- **适配场景**：适配Windows10/11全系系统，兼容老旧办公电脑显示效果

## 三、全局视觉规范（统一全局样式）

### 1\. 配色体系（主色\+辅助色\+中性色）

采用低饱和静谧蓝为主色调，适配办公场景，不刺眼、高级耐看，全程无花哨渐变

- **主色（品牌蓝）**：\#1677FF（默认按钮、选中状态、高亮文字、进度条主色）

- **主色hover**：\#4096FF（按钮悬浮提亮反馈）

- **成功色**：\#52C41A（转换成功状态）

- **警告色**：\#FAAD14（待转换、等待中状态）

- **错误色**：\#F5222D（转换失败、异常状态）

- **中性黑**：\#1F2329（标题、主要文字）

- **中性灰1**：\#4E5969（常规正文文字）

- **中性灰2**：\#86909C（辅助文字、提示文字）

- **边框灰**：\#E5E6EB（卡片、输入框、分割线边框）

- **页面背景**：\#F7F8FA（全局页面底色，柔和不刺眼）

- **卡片背景**：\#FFFFFF（纯白卡片，分层清晰）

### 2\. 圆角规范（全局统一，Figma标准）

- 大卡片（拖拽区、设置面板、列表容器）：8px 圆角

- 常规控件（按钮、输入框、下拉框、弹窗）：6px 圆角

- 迷你标签（状态标签、角标）：4px 圆角

- 禁止直角、超大圆角，保持统一轻量化质感

### 3\. 阴影规范（轻阴影，无厚重黑影）

- 卡片阴影：偏移0px 2px 8px，透明度8%，模糊度6px

- 悬浮高亮阴影：偏移0px 4px 12px，透明度12%，悬浮触发

- 控件无默认阴影，仅卡片、悬浮状态启用阴影，保证界面干净

### 4\. 字体规范（全系统统一，无乱码适配）

- 首选字体：Segoe UI（Windows原生现代字体）

- 兜底字体：微软雅黑、宋体（适配老旧系统）

- 标题字号：16px（加粗）

- 正文字号：14px（常规权重）

- 辅助提示字号：12px（浅灰色）

### 5\. 间距规范（8px栅格系统，Figma标准）

- 最小基础间距：8px

- 卡片内边距：16px

- 模块间距：24px

- 控件上下间距：12px

- 文字行间距：1\.5倍

## 四、页面整体布局结构（单页一体式布局）

全程单页面无跳转，分区清晰、层级分明，符合办公工具操作习惯，布局分为五大模块

### 1\. 顶部导航栏（固定顶部）

- 左侧：软件Logo \+ 产品名称「轻文 · Word批量转PDF工具」

- 中间：空白留白，提升高级感

- 右侧：版本号、关于按钮、最小化按钮

- 样式：纯白背景，底部1px浅灰分割线，高度56px

### 2\. 左上核心拖拽区（核心功能区）

超大可视化卡片，用户第一视觉焦点，主打便捷操作

- 展示图标：上传/文档图标（轻量化线性图标）

- 主文案：「拖拽Word文件/文件夹到此处」

- 辅助文案：「支持 doc、docx 格式，纯本地离线转换」

- 功能按钮：「选择文件」「选择文件夹」两个主按钮

- 交互：拖拽悬浮卡片高亮，支持批量导入，空状态友好提示

### 3\. 左下文件列表区（数据展示区）

展示所有待转换、转换中、已完成、失败的文件列表

- 列表字段：文件名、文件格式、转换状态、操作

- 状态标签：待转换（灰）、转换中（蓝）、成功（绿）、失败（红）

- 功能按钮：单项重新转换、清空列表

- 样式：无默认表格边框，行悬浮高亮，极简列表样式

### 4\. 右侧配置面板（功能设置区）

独立卡片容器，收纳所有配置项，不占用主操作区

- 面板标题：转换设置

- 配置项1：输出目录选择（自定义/原目录输出）

- 配置项2：是否递归扫描子文件夹

- 配置项3：重复文件覆盖策略（覆盖/跳过）

- 配置项4：转换完成自动打开输出文件夹

### 5\. 底部状态栏（全局状态展示）

- 左侧：实时日志输出框（展示转换进度、失败原因、系统提示）

- 中间：整体进度条（展示批量转换总进度）

- 右侧：统计数据（总数/成功数/失败数）\+「开始转换」主按钮

## 五、核心控件UI规范（可直接复刻XAML样式）

### 1\. 主按钮 / 次按钮

- 主按钮：主色填充、白色文字、6px圆角、无边框、悬浮提亮

- 次按钮：白色背景、主色文字、主色细边框、6px圆角、悬浮浅底色

- 禁用状态：低透明度、不可点击、灰色显示

### 2\. 输入框 / 下拉框

- 默认状态：白色背景、浅灰边框、6px圆角、无阴影

- 聚焦状态：边框变为主色，轻微高亮

- hover状态：边框轻微加深，交互反馈柔和

### 3\. 卡片容器

- 底色纯白、8px圆角、轻阴影、16px统一内边距

- 无多余装饰、无渐变、无复杂纹理，极简高级

### 4\. 进度条

- 底色浅灰，进度条为主色，圆角全覆盖

- 进度实时平滑过渡，无卡顿突兀效果

### 5\. 状态标签

- 纯色浅底色\+对应文字色，4px圆角、无边框、小号字体

- 成功：浅绿底\+绿色文字；失败：浅红底\+红色文字；等待：浅黄底\+黄色文字

## 六、核心交互规范（提升用户体验）

- **拖拽交互**：拖拽文件进入区域，卡片高亮提示；拖拽离开恢复默认状态

- **加载交互**：转换过程中按钮禁用，防止重复点击，界面无卡死（异步交互）

- **状态实时更新**：文件状态、进度条、统计数据实时刷新，无延迟

- **错误友好提示**：转换失败不弹窗报错，仅日志记录\+状态标红，不打断批量任务

- **悬浮反馈**：所有可点击控件均有hover样式变化，交互感知清晰

- **空状态处理**：无文件时展示空状态文案\+图标，界面不空白突兀

## 七、适配与兼容规范

- **分辨率适配**：适配1080P、2K主流屏幕，窗口大小固定最优比例，可微小缩放

- **系统兼容**：完美适配Windows10/Windows11，老旧系统无样式错乱

- **字体兼容**：多级字体兜底策略，杜绝字体缺失导致的排版错乱

- **高DPI适配**：支持系统高缩放比例，控件、文字无模糊、无错位

## 八、设计落地说明

- 本规范完全适配 **WPF \+ HandyControl** 技术栈，所有样式可直接通过全局样式重写覆盖，无需自定义控件模板

- 所有配色、圆角、间距统一标准化，开发无需自行调整样式，保证界面高度统一

- 设计无过度效果，兼顾颜值与软件运行性能，低配电脑也能流畅运行

- 完全区别市面免费粗糙工具，视觉质感支撑9\.9元付费定价，提升用户购买意愿

> （注：部分内容可能由 AI 生成）
